# Vivim：基于视频视觉Mamba的医疗视频分割

## 摘要

由于视频帧中存在冗余的动态信息，医学视频分割在临床实践中日益受到关注。然而，传统的卷积神经网络（CNN）感受野有限，而基于Transformer的网络在构建长程依赖时面临计算复杂度过高的问题。这一瓶颈在处理内存有限的医学视频分析任务及长序列时构成了重大挑战。近年来，以Mamba为代表的状态空间模型（SSM）在高效长序列建模方面取得了显著成就，通过显著扩展深度神经网络的感受野，推动了众多视觉任务的发展。遗憾的是，普通的SSM无法同时捕捉因果时间线索并保留非因果的空间信息。为此，本文提出了一个基于视频视觉Mamba的框架——Vivim，用于医学视频分割任务。我们的Vivim通过精心设计的时间Mamba块（Temporal Mamba Block），能够有效地将长期时空表征压缩成不同尺度的序列。我们还引入了跨帧改进的边界感知仿射约束（Boundary-Aware Affine Constraint），以增强Vivim在模糊病灶上的辨别能力。在甲状腺结节分割、超声视频乳腺病变分割以及结肠镜视频息肉分割上的广泛实验表明，我们的Vivim在性能和效率上均优于现有方法。

## I. 引言

病变和组织的自动分割对于计算机辅助临床检查和治疗至关重要[1]，例如超声病变分割、息肉分割等。然而，由于病灶边界模糊、分布不均、运动模式多样以及在复杂环境中存在动态变化等固有因素，医学对象的分割通常具有挑战性[2]。医学视频本质上是医学图像序列，为定位模糊病灶和组织提供了更丰富、更细致的背景。这些额外信息使基于视频的分割能够更好地应对意外的复杂情况，提供连续的视角，从而实现更准确、更全面的分析。因此，为了考虑更多的目标上下文，医学视频分析迫切希望扩展深层模型在时空域中的感受野。

传统的卷积神经网络（CNN）[3-6]与近期的基于Transformer的架构相比，往往难以捕捉全局信息。Transformer架构采用多头自注意力（MSA）[7]来提取全局信息，在通用视频目标分割社区引起了广泛关注[8-10]。考虑到相邻帧为分割提供了有益的提示，这些方法通常会引入一些关于自注意力机制的专用模块来利用时间信息。然而，探索额外的时间维度往往会导致复杂度和资源需求的激增，这对实施提出了重大挑战，尤其是考虑到医疗视频的严格环境条件和固有的高维特性。例如，引入时间自注意力模块可能会无意中导致复杂度随时间维度呈二次方增长，从而带来巨大的计算挑战。在长视频序列中，token数量的显著增加，使得在采用多头自注意力（MSA）技术进行时间信息建模时面临相当大的计算压力[9]。

最近，为了解决长序列建模中出现的问题，受状态空间模型（SSM）[12]启发，Mamba[11]被提出。其主要思想是通过实现选择性扫描机制来高效捕捉长距离依赖关系。基于此，U-Mamba[13]设计了一个混合CNN-SSM模块，主要由Mamba模块组成，用于处理生物医学图像分割任务中的长序列。Vision Mamba[14]在图像分类和语义分割任务中，通过采用双向Mamba模块，提供了新的通用视觉骨干。正如他们所建议的，依赖自注意力模块并非实现高效视觉表征学习的必要条件。在探索视频场景中的长期时间依赖时，它可以被Mamba替代。将Vision Mamba模型应用于视频应用的关键在于能够同时捕捉因果时间线索，同时保持非因果空间信息的完整性。

!placeholder  
**图1：** 我们收集的VTUS数据集中的若干案例。所有视频均来自甲状腺结节患者的超声检查。这些视频由拥有超过10年临床经验的超声医生拍摄，以确保图像质量。这些视频由三位拥有三年以上甲状腺诊断经验的专家进行了交叉注释。

基于此，我们提出了基于SSM的框架Vivim，将Mamba集成到多级Transformer架构中，利用线性复杂度的优势处理视频中的时空信息。据我们所知，这是首项将SSM融入医学视频分割任务的工作，促进了更快更强的性能。在我们的Vivim中，借鉴现代Transformer模块架构，设计了一种新颖的时空Mamba模块。引入了由多个时间Mamba块组成的分层编码器，以研究不同尺度下空间与时间依赖性的相关性。作为结构化状态空间序列的选择性扫描模型（S6）[11]，其在处理因果输入数据时，只能捕捉扫描数据部分的信息。这使得S6与涉及时间数据的自然语言处理和视频任务相契合，但在处理非因果数据（如医疗视频中的二维图像）时存在挑战。为此，我们设计并集成了带有时空选择扫描（Spatiotemporal Selective Scan）的结构化状态空间序列模型ST-Mamba，取代了自注意力或窗口注意力模块，实现高效的视频视觉表征学习。最后，我们采用改进的边界感知仿射约束，提升Vivim在医学视频中模糊组织的识别能力。

值得注意的是，目前没有包含像素级标注的超声视频用于甲状腺分割的公开数据集，因为在低对比度超声视频中逐帧划定模糊病灶边界成本高昂。本研究收集了甲状腺分割数据集VTUS，包含100个带注释的横切面和纵切面超声视频，以及9342帧像素级的真值数据，以便于基准测试。图1展示了若干示例。我们对三种常见的医学视频分割任务进行了广泛实验，即超声视频中的甲状腺分割、超声视频中的乳腺病变分割和结肠镜视频中的息肉分割。这些优越的结果验证了我们Vivim框架的有效性、效率和多样性。

**我们的贡献总结如下：**

- 我们开发了由基于Mamba编码器和基于CNN的解码器组成的医学视频分割框架，分别实现对医疗视频的整体理解和局部细节的保存。这是首次将状态空间模型引入医学视频场景的工作。
- 我们不仅仅将Mamba应用于医疗任务，而是设计了时空选择扫描机制，以增强我们时间Mamba块在视频中的全局感知能力。
- 我们采用基于仿射变换优化的改进边界感知约束，以减少模型边界预测的模糊性。
- 我们收集了首个带有像素级注释的视频超声甲状腺分割数据集，便于医学视频分割方法的基准评估。我们的模型在多种模态上实现了有前景的分割效果，同时保持了优于基于Transformer方法的良好效率。

## II. 相关工作

### II-A 医学视频分割

近期方法引入了创新的混合Transformer算法，融合Transformer层和卷积层技术用于医学图像分割（如乳腺病变、息肉）[15-20]。关于超声图像中的甲状腺分割，Jeremy等人[21]开发了一种新型时空循环深度学习网络，利用时间序列信息自动分割超声Cine clip中的甲状腺。Ma等人[22]利用区域提议网络（RPN）进行初步深度特征提取，并将空间金字塔RoIAlign作为分割头，捕捉超声图像中的全局和局部信息。Chi等人[23]开发了一种二维Transformer-UNet，用于甲状腺分割，利用多尺度交叉注意力Transformer模块将解码层的高级特征与编码层的低层特征结合起来。这些算法能够熟练地处理高清医学图像的表示，但由于复杂性问题，它们面临计算困难。此外，直接应用此类图像分割方法可能无意中忽略关键的时间上下文，从而引发时间上的不一致。

为了解决视频级分割中的时间建模，创新的方法——时空记忆网络（STM）[24]及其变体[25, 10, 26]引入了记忆网络，从由所有之前视频序列组成的时间缓冲区中提取关键信息。基于此方法，DPSTT[27]集成记忆库与解耦Transformer，用于追踪医学超声视频中的病灶运动。然而，DPSTT需要大量数据增强以避免过拟合，且处理速度较慢，凸显了一些潜在局限。FLA-Net[28]提供一个频率和位置特征聚合网络，具有较大的内存占用量，用于超声视频乳腺病变分割。因此，医学视频分割的挑战在于如何高效利用丰富的时间数据。

### II-B 状态空间模型

最近，状态空间模型（SSM）[12]在利用状态空间变换方面已表现出显著的效率[29]，能够管理语言序列中的长期依赖关系。S4[30]引入了结构化状态空间序列模型，利用线性复杂度捕捉长程依赖性。基于此，Mamba[11]集成了高效的硬件设计和采用并行扫描（S6）的选择机制，从而在处理大量自然语言序列时超越了Transformers。随后，S4ND[31]探索了SSM对多维数据（如图像和视频）的连续信号建模。最近，Vision Mamba[14]以及VMamba[32]开创了通用视觉任务，并在有效性和效率上超越了基于Transformer的方法，引入了双向扫描和交叉扫描机制，以解决SSM中的方向敏感性挑战。U-Mamba[13]设计了一个混合CNN-SSM模块，主要由Mamba模块组成，用于处理生物医学图像分割任务中的长序列。据我们所知，SSM尚未在医学视频分割任务中被探索。

!placeholder  
**图2：** (a) 拟议的Vivim医学视频分割概览。视频序列首先输入补丁嵌入和多尺度时间Mamba模块进行编码。然后，特征序列被聚合，通过基于CNN的分割头预测分割结果。(b) Vivim的基本构建模块，即时间Mamba模块。Efficient Spatial Self-attention进行初步空间建模，ST-Mamba则以线性复杂性探索时空依赖性。(c) ST-Mamba采用时空选择扫描，实现视频视觉任务的长序列建模。

## III. 方法

### III-A 概述

在本部分，我们将详细介绍基于Mamba的Vivim解决方案，用于医学视频分割任务。我们的Vivim主要由两个模块组成：一个带有叠加时间Mamba块的分层编码器，用于提取不同尺度下的粗细特征序列；以及一个基于CNN的轻量级分割头，用于融合多层特征序列并预测分割掩码。图2展示了我们拟议的Vivim流程图。具体来说，给定一个包含 $T$ 帧的视频片段 $\mathbf{V} = \{I_1, \dots, I_T\}$，我们首先通过重叠的补丁嵌入（Patch Embedding）将这些帧划分为大小为 $4 \times 4$ 的补丁。然后我们将补丁序列输入分层的时间Mamba编码器，以获得多层次时空特征，其分辨率分别为原始帧的 $\{1/4, 1/8, 1/16, 1/32\}$。最后，我们将多层特征传递给基于CNN的分割头，以预测分割结果。边界感知仿射约束仅在训练过程中部署，如图4所示。

### III-B 预备知识：状态空间模型

状态空间模型（SSMs）通常被视为线性时不变系统，通过隐藏状态 $\mathbf{h}(t) \in \mathbb{R}^N$，将一维函数或序列 $x(t) \in \mathbb{R}$ 映射到 $y(t) \in \mathbb{R}$。该系统通常被表述为线性常微分方程（ODE）：

$$
\begin{aligned}
\mathbf{h}'(t) &= \mathbf{A}\mathbf{h}(t) + \mathbf{B}x(t), \\
y(t) &= \mathbf{C}\mathbf{h}(t).
\end{aligned}
\tag{1}
$$

其中 $\mathbf{A} \in \mathbb{R}^{N \times N}$ 是演化参数，$\mathbf{B} \in \mathbb{R}^{N \times 1}$、$\mathbf{C} \in \mathbb{R}^{1 \times N}$ 是投影参数。

引入离散化主要是为了将ODE转换为离散函数。这种转换对于使模型与输入数据中体现的底层信号的采样率对齐至关重要。结构化状态空间序列模型（S4）和Mamba是连续系统的经典离散版本，它们包含时间尺度参数 $\Delta$ 以将连续参数 $\mathbf{A}, \mathbf{B}$ 转换为离散参数 $\overline{\mathbf{A}}, \overline{\mathbf{B}}$。常用的转换方法是零阶保持（ZOH）：

$$
\overline{\mathbf{A}} = \exp(\Delta \mathbf{A}), \quad \overline{\mathbf{B}} = (\Delta \mathbf{A})^{-1}(\exp(\Delta \mathbf{A}) - \mathbf{I}) \cdot \Delta \mathbf{B}.
\tag{2}
$$

离散化后，式(1)可重写为：

$$
\begin{aligned}
\mathbf{h}_t &= \overline{\mathbf{A}}\mathbf{h}_{t-1} + \overline{\mathbf{B}}\mathbf{x}_t, \\
\mathbf{y}_t &= \mathbf{C}\mathbf{h}_t.
\end{aligned}
\tag{3}
$$

最后，模型通过全局卷积计算输出：

$$
\overline{\mathbf{K}} = (\mathbf{C}\overline{\mathbf{B}}, \mathbf{C}\overline{\mathbf{A}}\overline{\mathbf{B}}, \dots, \mathbf{C}\overline{\mathbf{A}}^{M-1}\overline{\mathbf{B}}), \quad \mathbf{y} = \mathbf{x} * \overline{\mathbf{K}},
\tag{4}
$$

其中 $M$ 是输入序列 $\mathbf{x}$ 的长度，$\overline{\mathbf{K}} \in \mathbb{R}^M$ 是一个结构化卷积核。

### III-C 整体架构

#### III-C1 分层特征表示

多层次特征提供了高分辨率的粗粒度特征和低分辨率的细粒度特征，显著改善了分割结果，特别是对于医学图像。为此，与Vivit[9]不同，我们的编码器在给定输入视频帧的情况下提取多层次多尺度特征。具体来说，我们在每个时间Mamba块的末尾逐帧执行补丁合并（Patch Merging），生成第 $i$ 个特征嵌入 $\mathcal{F}_i$，其分辨率为 $H/2^{i+1} \times W/2^{i+1}$。

#### III-C2 时间Mamba块

探索时间信息对于医学视频分割至关重要，因为它提供了动态外观和运动线索。然而，普通Transformer架构中的MSA与token数量呈二次复杂度[7]。这种复杂度对于来自视频的长特征序列尤为重要，因为token数量随输入帧数线性增加。受此启发，我们开发了一个更高效的模块——时间Mamba块，利用结构化状态空间序列模型同时挖掘空间和时间信息。

如图2(b)所示，在时间Mamba块中，首先引入高效的仅空间自注意力模块（Efficient Spatial Self-attention）以提供空间信息的初始聚合，随后是混合前馈层（Mix-FeedForward layer）。我们利用[33, 34]中引入的序列缩减过程来提高其效率。对于给定视频片段的第 $i$ 级特征嵌入 $\mathcal{F}_i \in \mathbb{R}^{T \times C_i \times H \times W}$，我们转置通道和时间维度，并将时空特征嵌入展平为一维长序列 $\mathbf{h}_i \in \mathbb{R}^{C_i \times T H W}$。然后，展平的序列 $\mathbf{h}_i$ 被送入时空Mamba模块（ST-Mamba）和细节特定前馈网络（DSF）层。ST-Mamba模块建立帧内和帧间的长程依赖，而DSF则通过核大小为 $3 \times 3 \times 3$ 的深度卷积保留细粒度细节。堆叠Mamba层中的过程定义为（其中 $l \in [1, N_m]$）：

$$
\begin{aligned}
\mathbf{h}_l &= \text{ST-Mamba}(\text{LN}(\mathbf{h}_{l-1})) + \mathbf{h}_{l-1}, \\
\mathbf{h}_l &= \text{DSF}(\text{LN}(\mathbf{h}_l)) + \mathbf{h}_l.
\end{aligned}
$$

最后，我们将输出特征序列返回到原始形状，并采用重叠补丁合并来对特征嵌入进行下采样。

#### III-C3 解码器

为了从多层次特征嵌入中预测分割掩码，我们引入了一个基于CNN的分割头。虽然我们的分层时间Mamba编码器在空间和时态轴上都具有大的有效感受野，但基于CNN的分割头进一步细化了局部区域的细节。具体来说，来自时间Mamba块的多层次特征 $\{\mathcal{F}_1, \mathcal{F}_2, \mathcal{F}_3, \mathcal{F}_4\}$ 被传入一个MLP层以统一通道维度。这些统一的特征被上采样到相同的分辨率并拼接在一起。第三，采用MLP层融合拼接后的特征 $\mathcal{F}$。最后，融合后的特征经过一个 $1 \times 1$ 卷积层以预测分割掩码 $\mathcal{M}$。训练期间应用由像素级交叉熵损失和IoU损失组成的分割损失 $\mathcal{L}_{seg}$。

!placeholder  
**图3：** 提出的时空选择扫描示意图，包括时间前向扫描、时间后向扫描和空间扫描。

### III-D 时空选择扫描

尽管S6对时间数据具有因果性，但视频与文本的不同之处在于，视频不仅包含时间上的冗余信息，还积累了非因果的二维空间信息。为了解决适应非因果数据并全面探索时间信息的问题，我们引入了如图2(c)所示的ST-Mamba，它将视频视觉任务的时空序列建模结合起来。

具体来说，为了明确探讨帧之间的关系，我们首先将每一帧沿行和列展开成序列，然后将帧序列串联成时间优先序列 $\mathbf{h}_i^t \in \mathbb{R}^{C_i \times T(HW)}$。我们并行进行正向和后向扫描，探索双向时间依赖性。这种方法使模型能够相互补偿对方的感受野，而不会显著增加计算复杂度。同时，我们沿时间轴堆叠补丁，构造空间优先序列 $\mathbf{h}_i^s \in \mathbb{R}^{C_i \times (HW)T}$。我们继续扫描，整合所有帧中每个像素的信息。图3生动展示了具有三个方向的时空选择扫描机制。我们的机制明确考虑单帧空间相干性和跨帧相干性，并利用并行SSM建立帧内和帧间长距离依赖关系。

**计算效率。** ST-Mamba中的SSM和Transformer中的自注意力都为自适应建模时空上下文提供了关键解决方案。给定视频视觉序列 $\mathbf{K} \in \mathbb{R}^{1 \times T \times M \times D}$，全局自注意和SSM的计算复杂度为：

$$
\begin{aligned}
\Omega(\text{Self-Attention}) &= 4(TM)D^2 + 2(TM)^2 D, \\
\Omega(\text{SSM}) &= 4(TM)(2D)N + (TM)(2D)N^2,
\end{aligned}
\tag{6, 7}
$$

其中默认膨胀比为2，$N$ 是固定参数，设为16。如前所述，自注意力与整个视频序列长度的平方 $(TM)$ 成正比，而SSM则是线性的。这种计算效率使ST-Mamba成为长期视频应用的更好解决方案，这一点也得到了第IV-E节对ST-Mamba效率的实验分析所验证。

!placeholder  
**图4：** 训练策略概述。具体来说，我们提出的补丁级边界感知仿射约束 $\mathcal{L}_{affine}$ 引入以优化Vivim，与分割损失 $\mathcal{L}_{seg}$ 以及边界交叉熵损失 $\mathcal{L}_{bce}$ 共同使用。用于计算仿射变换的预训练MLP在训练过程中被冻结。

### III-E 边界感知仿射约束

仅通过分割监督优化的网络往往会产生模糊且无结构的预测，并在训练数据上出现过拟合。为缓解这些问题，我们引入了受InverseForm启发的补丁级边界感知仿射约束[35]，以强制执行预测的边界结构。具体来说，如图4所示，我们通过优化特征图中真值边界和预测边界之间的仿射变换，以实现恒定变换矩阵，从而解决这一约束任务。

补丁中的真值边界是通过在真值掩码上应用Sobel算子[36]得出的，而由三个卷积层组成的辅助边界头处理Mamba编码器的特征片段以获得预测的边界。我们计算仿射变换矩阵 $\hat{\theta}_i^t$，对应于第 $i$ 个补丁的真值边界 $B_{gt}^t$ 和预测边界 $B_{pred}^t$，针对视频片段中的目标帧 $I_t$，由一个预训练的MLP处理。同时，我们计算另一个仿射变换矩阵 $\hat{\theta}_i^1$，对应于第 $i$ 个补丁的真值边界 $B_{gt}^1$（关于 $I_1$）和预测边界 $B_{pred}^t$（关于 $I_t$）。该MLP事先用边缘掩码训练，且在我们方法训练时未进行优化。我们优化矩阵 $\hat{\theta}_i^t$，并对抗性优化 $\hat{\theta}_i^1$ 趋向恒定矩阵 $\mathbb{I}$：

$$
\mathcal{L}_{affine} = \frac{1}{N_p} \sum_{i=1}^{N_p} (\Delta_1 \cdot |\hat{\theta}_i^t - \mathbb{I}|_F - \Delta_2 \cdot |\hat{\theta}_i^1 - \mathbb{I}|_F),
\tag{8}
$$

其中 $N_p$ 表示补丁数，$|\cdot|_F$ 是Frobenius范数。$\Delta_1$ 和 $\Delta_2$ 是两个用于控制 $\hat{\theta}_i^t$ 和 $\hat{\theta}_i^1$ 效应的平衡超参数，经验上设为1.00和0.01。在这个目标中，$B_{pred}^t$ 被推向 $B_{gt}^t$ 并拉离 $B_{gt}^1$，从而改善目标边界并保持病灶结构中细微的帧间差异。

我们还采用了二元交叉熵损失 $\mathcal{L}_{bce}$ 将整个预测边界与目标帧的对应真值结合，以进一步优化边界检测。最后，训练期间的整体优化损失如下，缩放参数 $\lambda_1, \lambda_2$ 两者经验上都设为0.3：

$$
\mathcal{L}_{total} = \mathcal{L}_{seg} + \lambda_1 \mathcal{L}_{affine} + \lambda_2 \mathcal{L}_{bce}.
\tag{9}
$$

## V. 讨论

我们的Vivim围绕放射科医生的诊断过程设计，通过ST-Mamba获得整体理解，并通过基于CNN的解码器聚合多尺度特征和边界约束，保持局部细节。尽管CNN和Transformer在许多超声视频分割任务中取得了令人印象深刻的性能[27, 28]，但在效率和效能方面仍有显著提升潜力。限制CNN和Transformer在医疗视频分析中更广泛应用的一个关键挑战是感受野与计算复杂性之间的权衡。这一问题源于CNN本质上的局部处理特性以及Transformer相关的高计算复杂性。状态空间模型（SSM），例如Mamba，相比Transformer提供了更高效的全局依赖建模技术，便于在超声视频中进行更动态的引用。超声专家通常通过使用横切面和纵切面视图来获取目标组织的完整外观，这些视图由高帧率设备捕捉。这也促使超声视频需要长序列建模。与Transformer中的自注意力机制[7, 9]（其与视频序列长度成平方扩展）不同，SSM的计算复杂度呈线性增长。这种线性可扩展性使SSM非常适合时空联合建模，使其能够在有限的内存和计算资源限制下运行，这对Transformer来说尤其具有挑战性，特别是在较长的视频序列中。

SSMs的因果性质与自然语言处理（NLP）和视频处理中的任务尤为契合[11]。将Mamba模型应用于医疗视频任务的一个关键挑战在于设计选择性扫描方向，以有效保留病变和组织的非因果空间细节，同时探索时间依赖性。因此，我们的方法超越了简单的Mamba应用；我们建立了一个基线，将Transformers用于空间建模和SSM用于时空建模结合起来。我们引入了一种三向扫描机制，能够同时沿时间前进、时间后退和空间前进方向工作，巧妙平衡跨帧相干性与单帧空间完整性。未来，我们将重点深入研究基于全SSM的高效医疗视频分割解决方案，并开发更具创新性的选择性扫描机制，针对医学视频分割任务量身定制，以更好地定位病灶位置。我们将努力在进行一维序列相互作用时更好地保持目标病灶邻近斑块之间的空间相关性。

## VI. 结论

本文提出了基于Mamba的Vivim框架，以解决医学视频分割的挑战，特别是在建模由于CNN固有的局部性和自注意力机制的高计算复杂性导致的长距离时间依赖性方面。Vivim的主要思想是将带有时空选择扫描ST-Mamba的结构化状态空间模型引入标准的分层Transformer架构中。这有助于以比自注意力机制更低廉的计算成本探索单帧空间相干性和跨帧相干性。在训练阶段提出了改进的边界感知约束，以缓解模型的模糊预测。我们还贡献了一个视频甲状腺超声分割数据集VTUS，包含100个视频和9342个注释帧。我们收集的VTUS数据集、超声乳腺病变视频和息肉结肠镜视频的实验结果显示，Vivim优于最先进的分割网络。消融研究还验证了ST-Mamba优于其他基于时空的Transformer方法。
