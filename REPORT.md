# 数字调制解调实验报告

**实验名称**：数字调制解调实验  
**学生姓名**：张桂嘉  
**学号**：2022040399  
**实验日期**：2026年6月2日  
**提交日期**：2026年6月2日

---

## 1. 实验目的

本实验的主要目的是理解数字通信中常见调制方式的基本原理，并使用 Python、NumPy 和 Matplotlib 完成 BPSK、QPSK、16-QAM 的调制、星座图绘制、解调以及误比特率性能分析。通过本实验，可以把课堂中的抽象映射关系转化为可运行的程序：比特序列先被映射为复数符号，符号经过噪声信道后再由判决算法恢复为比特。实验同时训练了科学计算环境配置、数组化编程、图像保存、自动测试和 AI 辅助编程的完整流程。

## 2. 实验原理

### 2.1 BPSK调制原理

BPSK（二进制相移键控）每个符号只携带 1 个比特，星座图上只有两个点，分别位于实轴的 +1 和 -1。实验中采用映射关系：比特 0 映射到 +1，比特 1 映射到 -1。由于两个点之间的欧氏距离较大，BPSK 的抗噪声性能较好，但频谱效率较低。

### 2.2 QPSK调制原理

QPSK 每 2 个比特映射为 1 个复数符号，星座点分布在单位圆上的四个象限。本实验采用格雷码映射：00 对应 `(1+1j)/sqrt(2)`，01 对应 `(-1+1j)/sqrt(2)`，11 对应 `(-1-1j)/sqrt(2)`，10 对应 `(1-1j)/sqrt(2)`。除以 `sqrt(2)` 后，每个符号能量为 1。格雷码的优点是相邻点只相差 1 个比特，噪声导致误判到相邻点时通常只产生 1 个比特错误。

### 2.3 16-QAM调制原理

16-QAM 每 4 个比特映射为 1 个复数符号，其中前 2 位决定 I 路分量，后 2 位决定 Q 路分量。I/Q 分量的取值为 `-3, -1, +1, +3`，本实验使用格雷码：00 到 +3，01 到 +1，11 到 -1，10 到 -3。因为未归一化的平均符号功率为 10，所以最终符号需要除以 `sqrt(10)`，使平均功率接近 1。16-QAM 频谱效率高，但星座点间距更小，因此在低 SNR 下更容易受到噪声影响。

## 3. 实验方法与步骤

### 3.1 环境配置

实验环境使用 Python 3、NumPy、SciPy、Matplotlib、pytest 和 pylint。首先运行环境测试脚本，确认 Python 版本和依赖包可用；然后在 `src/modulation.py` 中实现三种调制函数，在 `src/demodulation.py` 中实现三种解调函数，最后运行测试脚本生成 `results/` 目录下的星座图和 BER 曲线。

### 3.2 BPSK实现

BPSK 的实现利用 NumPy 向量化运算完成。对于输入数组 `bits`，表达式 `1 - 2 * bits` 可以把 0 转换为 +1，把 1 转换为 -1，并统一转换为复数类型以便后续接口一致。

```python
def bpsk_modulate(bits):
    bits = np.asarray(bits, dtype=int)
    symbols = (1 - 2 * bits).astype(complex)
    return symbols
```

### 3.3 QPSK实现

QPSK 首先检查输入长度是否为偶数，然后使用 `reshape(-1, 2)` 把比特按两位分组。每个二元组通过格雷码字典映射到对应的复数点，最后除以 `sqrt(2)` 完成单位能量归一化。

### 3.4 16-QAM实现

16-QAM 首先检查输入长度是否为 4 的倍数，然后使用 `reshape(-1, 4)` 把比特分成四位一组。前两位映射实部，后两位映射虚部，I/Q 分量均采用同一个格雷码映射字典。最终符号除以 `sqrt(10)`，保证平均功率为 1。

### 3.5 解调与BER测试

BPSK 解调只需要判断接收符号实部是否大于 0。QPSK 解调用最小欧氏距离判决，将接收符号与四个理想星座点比较。16-QAM 解调分别对实部和虚部判决到最近的四个电平，再恢复对应的比特对。BER 测试流程为：随机生成比特、调制、添加 AWGN 噪声、解调、比较发送和接收比特并计算误比特率。

## 4. 实验结果

### 4.1 BPSK星座图

![BPSK星座图](results/bpsk_constellation.png)

BPSK 星座图中可以观察到两个点位于实轴两侧，虚部接近 0，符合二进制相移键控的理论特征。

### 4.2 QPSK星座图

![QPSK星座图](results/qpsk_constellation.png)

QPSK 星座图中四个点均匀分布在四个象限，点的幅度为 1，说明归一化处理正确。

### 4.3 16-QAM星座图

![16-QAM星座图](results/16qam_constellation.png)

16-QAM 星座图呈 4×4 方格分布，实部和虚部的取值均来自 `{-3, -1, +1, +3}/sqrt(10)`，满足实验要求。

### 4.4 BER性能曲线

![BER性能曲线](results/ber_comparison.png)

BER 曲线反映了 SNR 增大时误比特率下降的趋势。通常 BPSK 和 QPSK 在相同平均符号功率下具有较好的抗噪声性能，而 16-QAM 因为星座点距离较近，在低信噪比区域更容易出现判决错误。

## 5. 结果分析与讨论

### 5.1 星座图对比分析

三种调制方式的星座点数量分别为 2、4、16。随着每个符号承载的比特数增加，频谱效率提高，但相邻星座点距离减小，对信道噪声更加敏感。BPSK 的实现和判决最简单，适用于可靠性优先的场景；QPSK 在效率和鲁棒性之间较平衡；16-QAM 可以在同样符号速率下传输更多比特，但需要更高的 SNR 才能达到较低 BER。

### 5.2 性能对比分析

从理论和仿真结果看，AWGN 信道下误比特率主要由信号点间距离和噪声功率决定。SNR 越高，接收符号越集中在理想星座点附近，解调错误概率越低。16-QAM 的角点和内点分布较密集，在同等噪声水平下更容易跨越判决边界，因此低 SNR 时 BER 通常高于 BPSK/QPSK。

### 5.3 遇到的问题与解决方法

实验中需要特别注意归一化因子的选择。QPSK 如果不除以 `sqrt(2)`，符号功率会变成 2；16-QAM 如果不除以 `sqrt(10)`，平均功率会变成 10，导致 BER 对比不公平。另一个容易出错的地方是 16-QAM 的格雷码顺序，需要确保 00、01、11、10 分别对应 +3、+1、-1、-3。通过自动测试检查映射、幅度、平均功率和星座点数量，可以快速定位这些问题。

## 6. 实验心得与Copilot使用体会

通过本实验，我更加清楚地理解了数字调制不是简单的画点，而是“比特分组、符号映射、功率归一化、信道传输、判决恢复”的完整链路。NumPy 的向量化写法让代码更简洁，也减少了循环中可能出现的索引错误。AI 助手在生成代码框架、解释报错、提示测试思路方面很有帮助，但映射规则、归一化常数、判决边界等关键细节仍需要人工根据通信原理核对。后续可以进一步增加理论 BER 曲线，与仿真曲线进行更深入的比较。

## 7. 参考文献

1. John G. Proakis, Masoud Salehi. 《数字通信（第五版）》. 电子工业出版社, 2011.
2. Simon Haykin. Communication Systems. Wiley, 2001.
3. NumPy 官方文档：https://numpy.org/doc/
4. Matplotlib 官方文档：https://matplotlib.org/stable/
5. 本实验仓库说明文档：https://github.com/jwentong/wireless-modulation-experiment

---

**声明**：本实验报告内容真实，代码根据实验要求独立实现，并使用自动测试进行验证。
