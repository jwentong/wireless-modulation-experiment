# 实验报告

**实验名称**：数字调制解调实验
**学生姓名**：廖祖颐
**学号**：2022110131
**实验日期**：2026年
**提交日期**：[提交日期]

---

## 1. 实验目的

本实验的目的是：
- 理解数字调制的基本原理（BPSK、QPSK、16-QAM）
- 掌握 BPSK、QPSK、16-QAM 调制算法的实现，并用 Python + NumPy 完成向量化编码
- 实现对应的最小欧氏距离解调算法，并通过误比特率（BER）验证调制解调的正确性
- 学习使用 Python 科学计算工具（NumPy、Matplotlib）进行信号处理与可视化
- 体验 AI 编程助手在调制解调代码实现中的辅助作用

---

## 2. 实验原理

### 2.1 BPSK 调制原理

BPSK（二进制相移键控）用两个相位相差 180° 的符号表示 1 比特信息，是最简单的相移键控方式。映射关系为：

$$
s =
\begin{cases}
+1, & \text{if } b = 0 \\
-1, & \text{if } b = 1
\end{cases}
$$

BPSK 的星座图只有两个点，分别位于实轴的 +1 和 -1 处。它的抗噪声性能在四种常见调制方式中最好（同样发送功率下误码率最低），但频谱效率最低——每个符号只能携带 1 bit 信息。

### 2.2 QPSK 调制原理

QPSK（四相相移键控）把 2 个比特编码进 1 个符号的相位，四个符号均匀分布在单位圆上（45°、135°、225°、315°），本实验采用格雷码映射：

$$
00 \to \frac{1+j}{\sqrt2},\quad 01 \to \frac{-1+j}{\sqrt2},\quad 11 \to \frac{-1-j}{\sqrt2},\quad 10 \to \frac{1-j}{\sqrt2}
$$

格雷码映射保证相邻星座点之间只有 1 个比特不同，这样一次判决错误（错判到相邻点）只会导致 1 个比特错误，而不是 2 个比特同时错误，能降低整体 BER。QPSK 的频谱效率是 BPSK 的 2 倍，但由于相邻符号间距比 BPSK 更小，抗噪声性能比 BPSK 差。

### 2.3 16-QAM 调制原理

16-QAM 把 4 个比特编码进 1 个符号，I（同相）和 Q（正交）两路分量各自独立取 4 个电平 {-3, -1, +1, +3}，组合成 4×4=16 个星座点。每路分量同样采用格雷码映射（00→+3, 01→+1, 11→-1, 10→-3）。由于平均符号功率为 $(3^2+1^2+1^2+3^2)/4=5$，需要除以 $\sqrt{10}$ 归一化到单位平均功率。16-QAM 的频谱效率是 QPSK 的 2 倍（BPSK 的 4 倍），但星座点间距进一步缩小，抗噪声性能是三种方式中最差的。

---

## 3. 实验方法与步骤

### 3.1 环境配置

使用 `pip install -r requirements.txt` 安装 numpy、scipy、matplotlib、pytest、pytest-cov、pylint，并运行 `python src/test_environment.py` 确认 Python 版本、依赖包、NumPy 运算和 Matplotlib 绘图均正常。

### 3.2 BPSK 实现

利用比特值和符号值之间 `+1/-1` 的线性关系，用一条向量化表达式完成映射，不需要循环：

```python
def bpsk_modulate(bits):
    symbols = (1 - 2 * bits).astype(complex)
    return symbols
```

比特为 0 时 `1-2*0=1`，比特为 1 时 `1-2*1=-1`，与要求的映射规则完全一致。

### 3.3 QPSK 实现

先将比特序列按 2 个一组 reshape，然后利用格雷码映射表可以化简成的代数关系（`real = 1-2*b1`, `imag = 1-2*b0`）一次性向量化算出全部符号，最后除以 √2 归一化：

```python
def qpsk_modulate(bits):
    if len(bits) % 2 != 0:
        raise ValueError("QPSK要求比特序列长度为偶数")
    pairs = np.asarray(bits).reshape(-1, 2)
    b0, b1 = pairs[:, 0], pairs[:, 1]
    real = 1 - 2 * b1
    imag = 1 - 2 * b0
    symbols = (real + 1j * imag) / np.sqrt(2)
    return symbols
```

### 3.4 16-QAM 实现

将比特序列按 4 个一组 reshape，前 2 位通过给定的 `gray_map` 字典查出 I 分量，后 2 位查出 Q 分量，组合成复数符号后除以 √10 归一化：

```python
def qam16_modulate(bits):
    groups = np.asarray(bits).reshape(-1, 4)
    norm = np.sqrt(10)
    symbols = np.zeros(len(groups), dtype=complex)
    for idx, group in enumerate(groups):
        i_val = gray_map[(int(group[0]), int(group[1]))]
        q_val = gray_map[(int(group[2]), int(group[3]))]
        symbols[idx] = (i_val + 1j * q_val) / norm
    return symbols
```

### 3.5 解调实现（选做）

BPSK 解调直接判断符号实部正负；QPSK 解调对每个接收符号计算到 4 个参考星座点的欧氏距离，取距离最小的点对应的比特对；16-QAM 解调把 I/Q 两路分开做独立的 4 电平判决（各自先判断正负，再判断幅值是否超过 2/√10 的门限），避免遍历全部 16 个参考点，计算效率更高。

### 3.6 性能测试（选做）

`performance_test.py` 中已提供的主循环依次对每个 SNR 值生成随机比特、调制、加 AWGN 噪声、解调、计算 BER，扫描范围为 0~14dB（步长 2dB），并将 BPSK/QPSK/16-QAM 三条曲线画在同一张对数坐标图上。

---

## 4. 实验结果

### 4.1 BPSK 星座图

![BPSK星座图](results/bpsk_constellation.png)

**分析**：星座图上只有两个点，分别位于 (+1, 0) 和 (-1, 0)，与理论完全一致，没有加噪声，因此点没有发散。

### 4.2 QPSK 星座图

![QPSK星座图](results/qpsk_constellation.png)

**分析**：四个点分别位于四个象限中心 (±0.707, ±0.707)，对应 45°/135°/225°/315° 四个相位，符号幅度均为 1（单位功率），与格雷码映射的理论位置完全吻合。

### 4.3 16-QAM 星座图

![16-QAM星座图](results/16qam_constellation.png)

**分析**：可以看到 4×4=16 个规则排列的星座点，角点（幅值最大）功率最高，中心附近的点功率最低，整体呈方形网格分布，与理论描述一致。

### 4.4 性能测试结果（选做）

![BER性能对比](results/ber_comparison.png)

实测数据（部分 SNR 点）：

| SNR (dB) | BPSK BER | QPSK BER | 16-QAM BER |
|---|---|---|---|
| 0 | 0.0827 | 0.1551 | 0.2865 |
| 4 | 0.0102 | 0.0560 | 0.1853 |
| 8 | 0.0002 | 0.0066 | 0.1029 |
| 12 | 0.0000 | 0.0001 | 0.0253 |
| 14 | 0.0000 | 0.0000 | 0.0086 |

**分析**：在相同 SNR 下，BER 排序始终是 BPSK < QPSK < 16-QAM，与理论预期完全一致——星座点间距越小（16-QAM 最密集），越容易被噪声干扰导致误判。

---

## 5. 结果分析与讨论

### 5.1 星座图对比分析

三种调制方式的星座图直观地反映了"频谱效率"与"抗噪声能力"之间的权衡：BPSK 只有 2 个点，彼此距离最远（间距 2），抗噪声能力最强，但每符号只能传 1 bit；16-QAM 有 16 个点，密集排列在同样的平均功率范围内，相邻点间距最小，抗噪声能力最弱，但每符号能传 4 bit。QPSK 介于两者之间。

### 5.2 性能对比分析

- **频谱效率**：16-QAM(4 bit/符号) > QPSK(2 bit/符号) > BPSK(1 bit/符号)
- **抗噪声性能（同样 SNR 下的 BER）**：BPSK > QPSK > 16-QAM（BPSK 最优）
- **实现复杂度**：BPSK 最简单（符号即实数判决）；QPSK 需要处理复数和两路正交分量；16-QAM 需要处理 4 电平的 I/Q 判决，复杂度最高但仍可以用两路独立判决简化

工程上通常根据信道质量动态选择调制方式（自适应调制）：信道好（SNR高）时用 16-QAM 提高吞吐量，信道差时降级到 QPSK 或 BPSK 保证可靠性。

### 5.3 遇到的问题与解决方法

1. **问题**：直接用循环实现 QPSK 比特对到符号的映射，代码冗长且容易在边界处出错。
   - **原因分析**：格雷码映射用 if/elif 逐一判断 4 种情况，代码可读性差，容易在移植到向量化实现时出错。
   - **解决方法**：观察到映射表满足 `real = 1-2*b1`, `imag = 1-2*b0` 的简单代数关系，改用 NumPy 向量化运算一次性算出全部符号，代码更简洁也更快。

2. **问题**：`utils.py` 里设置中文字体时写死了 Windows 专用字体列表（Microsoft YaHei 等），在非 Windows 环境下运行会出现中文显示为方块的警告。
   - **原因分析**：`font_manager` 找不到这些字体名称对应的字体文件。
   - **解决方法**：在 `setup_chinese_font()` 中增加字体候选列表并在设置前检测系统实际可用字体（新增 PingFang SC / Noto Sans CJK / WenQuanYi 等跨平台候选），使脚本在 Windows/Mac/Linux 下都能正确显示中文标签。

---

## 6. 实验心得与 AI 助手使用体会

### 6.1 实验心得

通过本次实验，对 BPSK/QPSK/16-QAM 三种调制方式的星座图结构、格雷码映射规则和抗噪声性能差异有了更直观、更定量的理解——尤其是通过实测 BER-SNR 曲线，直接看到了"星座点越密集、抗噪声能力越弱"这一理论结论在数据上的体现，比单纯看公式推导印象更深刻。同时也熟悉了用 NumPy 做批量信号处理（reshape、向量化运算）比逐比特循环处理更简洁高效。

### 6.2 AI 助手使用体会

在本次实验中，AI 助手主要帮助完成了：
- 梳理仓库结构、评分脚本和函数签名要求，避免因为接口不匹配导致自动评分失败；
- 把格雷码映射表转化成可向量化的代数表达式（`real=1-2*b1`, `imag=1-2*b0`），减少了逐条件判断的循环代码；
- 补充 16-QAM 解调时 I/Q 独立判决的门限逻辑，并用 pylint 检查代码规范，修正命名和多余空白等细节问题，把代码质量评分从 6.4 提升到 9.2。

AI 助手在"把清晰的数学关系转成规范的 NumPy 代码"和"发现代码风格问题"上效率很高，但调制方式的判决门限、格雷码映射的物理含义仍需要自己核对公式和星座图才能确认代码是否真的正确——这也是为什么在验收前又手动核对了每一个格雷码映射用例的原因。

### 6.3 改进建议

`utils.py` 里的中文字体设置建议默认就做跨平台检测，而不是只写 Windows 字体列表，可以减少大部分同学在 Mac/Linux 上第一次运行时看到警告和方块字的困惑。

---

## 7. 参考文献

1. John G. Proakis, Masoud Salehi. 《数字通信（第五版）》. 电子工业出版社, 2011.
2. [维基百科 - 相移键控](https://zh.wikipedia.org/wiki/%E7%9B%B8%E7%A7%BB%E9%94%AE%E6%8E%A7)
3. [NumPy官方文档](https://numpy.org/doc/)
4. 课程仓库 jwentong/wireless-modulation-experiment 的 README.md 与 docs/ 原理文档

---

**声明**：本实验报告内容真实，所有代码均为本人编写（或在AI助手辅助下完成），未抄袭他人成果。

**签名**：________
**日期**：________
